spring:
 application:
  name: problem-statement1

logging:
  level:    root: info  file: /var/log/apps/problem-statement1/problem-statement1

kafka:
  kafkaProducer:
    bootstrap.servers: '172.17.5.36:7092'
    acks: 'all'
    batch.size: 150000
    buffer.memory: 33554432
    client.id: 8
    retries: 1
    linger.ms: 10
    compression.type: 'lz4'
